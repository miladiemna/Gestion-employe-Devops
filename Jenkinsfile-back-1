pipeline {
    agent any
    tools{
        maven 'maven'
    }


    stages {
        stage('Clone Repository from git') {
            steps {
                sh 'rm-rf'
                sh 'git clone https://github.com/miladiemna/Gestion-employe-Devops.git'
            }
        } 

    }
}